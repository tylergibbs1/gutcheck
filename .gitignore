# Auto-research loop logs
run.log
logs/

# Experiment outputs (predictions, overlays, per-image metrics)
# These are regenerated by running the eval/render scripts; we commit a
# curated subset of visuals under docs/images/ instead.
results/

# Data & checkpoints — never committed, fetched via scripts/download_*.sh
data/
checkpoints/

# Local Python / editor cruft
__pycache__/
*.py[cod]
*.pt
*.bin
*.safetensors
.venv/
venv/
.ipynb_checkpoints/
.DS_Store

# Credentials — belt-and-braces in case someone adds one
*.env
.env
.hf_env
.huggingface/
